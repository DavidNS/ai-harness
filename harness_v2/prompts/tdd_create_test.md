# Prompt

Write only the focused test changes required to prove the task fails before implementation. The test should express the task acceptance criteria and be runnable by the focused test command supplied in tasks.json.

Return a concise text summary. Do not claim the controller has run tests; the controller owns test execution and evidence.
