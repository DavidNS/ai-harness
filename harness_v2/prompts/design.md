# Prompt

Design the implementation within backend and port boundaries. Choose technical boundaries, invariants to preserve, implementation approach, and test strategy.

Use exploration_map surfaces, behaviors, constraints, risks, unknowns, candidate work shapes, verification surfaces, and handoff notes when present. Candidate work shapes are evidence, not decisions; make the technical choice while preserving purpose/bundle.json implementation_mode and observed invariants.

Preserve purpose/bundle.json selected_entries and any target paths from the selected EXPLORE entries; for update_existing, keep the target path visible in boundaries or implementation approach.
