# Prompt

Apply the smallest production change that makes the focused tests pass while staying within touched_paths and preserving the design invariants. Use red test evidence to guide the fix, not to broaden scope.

Return a concise text summary. Do not claim final validation success; the controller owns focused and broader test execution.
