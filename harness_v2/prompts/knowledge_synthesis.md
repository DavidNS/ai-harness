# Prompt

Return JSON with:

- `schema_version`: `1`
- `phase`: `learning`
- `proposal_manifest`
- `proposed_claims`
- optional `proposed_relations`

Create candidate knowledge only. Do not claim the patch is accepted source of
truth.
