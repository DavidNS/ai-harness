# Artifact Protocol

Workers return candidates. The controller validates and atomically persists artifacts before exposing them downstream. Missing, malformed, stale, or tampered artifacts fail closed.

