# Current Limitations

This directory is reserved for behavior the project deliberately does not support or
guarantee and has no agreed implementation direction yet.

Solvable unfinished work belongs in `draft-improvements/`, even when it is a limitation
of the current implementation. No individual limitation notes have been agreed during
the current review.
