# Refactor Archive

This directory is historical evidence for completed product-refactor boundaries.
Do not read it during startup. Open files here only when a human request or the
tracked checkpoint names a specific historical record.

Process-maintenance records about startup-context cleanup, context-budget rules,
and refactor-folder status cleanup were intentionally pruned. The current
process behavior lives in `refactor/control/checkpoint.json`.
