I believe we missing concepts and focus in this repo.

We have an analysis path and then also the ssd which have an explorer phase.
Are we not resolving this in the wrong way? Is not actually the analysis path the real explorer concept?

Is not our target to actually improve all the ssd steps so EXPLORE, PURPOSE, DESIGN, IMPLEMENT... means something.

Maybe we we can do on this way instead:

- The code path means actually the ssd path.
- The ssd path have resolution leves: simple, medium, hard. Instead easy task or full ssd.
- The explorer path actually should be resolved in the way analysis behaves. This means explorer is replaced by the full analysis flow.
- We use the naming EXPLORER instead analysis. EXPLORER will be the ubiquitous language prefered.
- We tranform the output artifact of the EXPLORER to became the input artifact of the SDD flows. The output will be the explorer bundle. 

- Instead of create our doc/analisys docs. We create temporal folders in ai-harness. Then the ARCHIVE phase will later transform it into permanent files. We store the limitations as permanent. Improvemen files will be mainly burocracy files (we just want to learn code not jira). And we not interested in store each bullshit user says.

This way, from next on, on our target becomes on provide ssd steaps meaning by decomposing each steps in this sub pipelines were we figure out how actually improve that steps meaning.