Escalation generally requires from artifacts recreation.

For example: Purposal phase is the one that let the user takes decision orver explorer conclusions. So on this point if we allow them to rephrase it will mean to escalate to explorer and recreate everything for a situation may just be resolved with a patch.

Maybe we can improve this by modifing intermediate artifacts schema in a way allows us to decide if we want full recreation or just a patch.

So if the investigation concludes 4 of 5 statements are right. But one of them is bullshit. Send it to the user. User repy with valid counter argument. Then we could just recreate just that statement.